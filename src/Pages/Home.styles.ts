import styled from "styled-components";

export const HeadingTest = styled.p`
	font-family: "Poppins", sans-serif;
	color: ${(props) => props.theme.primaryText};
	font-weight: bold;
	font-size: 64px;
`;
